{
  "name": "health",
  "private": true,
  "description": "Monorepo root; Next.js app is the web workspace",
  "workspaces": ["web"],
  "scripts": {
    "build": "npm run build -w web",
    "dev": "npm run dev -w web",
    "lint": "npm run lint -w web"
  }
}
